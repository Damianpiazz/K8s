apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: frontend
  labels:
    app: frontend
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: frontend
