apiVersion: v1
kind: Service
metadata:
  name: recommendation-svc
  labels:
    app: recommendation-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: recommendation-svc
  ports:
    - name: http
      port: 8092
      targetPort: http