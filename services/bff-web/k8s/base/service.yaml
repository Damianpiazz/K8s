apiVersion: v1
kind: Service
metadata:
  name: bff-web
  labels:
    app: bff-web
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: bff-web
  ports:
    - name: http
      port: 8090
      targetPort: http