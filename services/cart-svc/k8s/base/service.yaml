apiVersion: v1
kind: Service
metadata:
  name: cart-svc
  labels:
    app: cart-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: cart-svc
  ports:
    - name: http
      port: 8082
      targetPort: http