apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api-gateway
  labels:
    app: api-gateway
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  # Single dev replica kept available during node maintenance.
  minAvailable: 1
  selector:
    matchLabels:
      app: api-gateway