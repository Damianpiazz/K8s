# Test profile: no Eureka, no external config server; Kafka consumer disabled.
eureka:
  client:
    enabled: false

spring:
  cloud:
    config:
      import-check:
        enabled: false

notifications:
  kafka:
    enabled: false