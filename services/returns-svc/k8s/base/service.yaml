apiVersion: v1
kind: Service
metadata:
  name: returns-svc
  labels:
    app: returns-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: returns-svc
  ports:
    - name: http
      port: 8095
      targetPort: http