apiVersion: v1
kind: Secret
metadata:
  name: keycloak-admin-credentials
  labels:
    app: auth
    app.kubernetes.io/part-of: ecommerce-platform
type: Opaque
# ⚠️ PLACEHOLDER ONLY — never commit real credentials.
# Production path: external-secrets → Azure Key Vault. See the sibling file
# external-secret.yaml (mirrors cluster/base/external-secrets/example-external-secret.yaml).
stringData:
  username: admin
  password: change-me-admin-password