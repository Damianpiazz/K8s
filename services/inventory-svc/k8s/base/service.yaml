apiVersion: v1
kind: Service
metadata:
  name: inventory-svc
  labels:
    app: inventory-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: inventory-svc
  ports:
    - name: http
      port: 8093
      targetPort: http