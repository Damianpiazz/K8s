apiVersion: v1
kind: Service
metadata:
  name: shipping-svc
  labels:
    app: shipping-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: shipping-svc
  ports:
    - name: http
      port: 8094
      targetPort: http