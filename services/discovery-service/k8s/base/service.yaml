apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  labels:
    app: discovery-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: discovery-service
  ports:
    - name: http
      port: 8761
      targetPort: http