apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-svc
  labels:
    app: payment-svc
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-svc
  template:
    metadata:
      labels:
        app: payment-svc
        env: production
        app.kubernetes.io/part-of: ecommerce-platform
    spec:
      serviceAccountName: payment-svc
      containers:
        - name: payment-svc
          image: acr.azurecr.io/payment-svc:latest
          ports:
            - name: http
              containerPort: 8085
          env:
            - name: JAVA_TOOL_OPTIONS
              value: -XX:MaxRAMPercentage=75.0
          envFrom:
            # Service defaults (datasource, eureka/config URLs).
            - configMapRef:
                name: payment-svc-config
            # Per-environment overrides (LOG_LEVEL, DB_HOST, DB_PORT, DB_NAME…).
            - configMapRef:
                name: ecommerce-env-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 3
          # Spring writes /tmp (embedded Tomcat) — keep the fs writable.
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL