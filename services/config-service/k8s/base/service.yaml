apiVersion: v1
kind: Service
metadata:
  name: config-service
  labels:
    app: config-service
    app.kubernetes.io/part-of: ecommerce-platform
spec:
  type: ClusterIP
  selector:
    app: config-service
  ports:
    - name: http
      port: 8888
      targetPort: http