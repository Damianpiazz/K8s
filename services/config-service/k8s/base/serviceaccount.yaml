apiVersion: v1
kind: ServiceAccount
metadata:
  name: config-service
  labels:
    app: config-service
    app.kubernetes.io/part-of: ecommerce-platform
# No automountToken = no pods carrying the API token by default.
automountServiceAccountToken: false